package qorrnsmj.smf.game.state.custom

// debugはstateにするとまずい？
// stateを同時に2つとかできない？
class DebugState {
    // Draw FPS, UPS and Context version
    //val height = renderer.getDebugTextHeight("Context")
    //renderer.drawDebugText("FPS: " + timer.getFPS() + " | UPS: " + timer.getUPS(), 5f, (5 + height).toFloat())
    //renderer.drawDebugText("Context: " + (if (isDefaultContext()) "3.2 core" else "2.1"), 5f, 5f)
}
